import { ViewContainerRef } from "@angular/core";
import { AudioOutletDirective } from "./audio-outlet.directive";

describe('AudioOutletDirective', () => {
  it('should create an instance', () => {
    //const directive = new AudioOutletDirective(new ViewContainerRef());
    //expect(directive).toBeTruthy();
  });
});
