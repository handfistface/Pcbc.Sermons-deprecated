import { SermonSeriesViewModel } from "./sermon-series-view-model";

export class SermonsViewModel {
  public sermonSeries: SermonSeriesViewModel[];

  public constructor(
  ) {
    this.sermonSeries = [];
  }
}
